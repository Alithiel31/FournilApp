DATABASE_URL="postgresql://fournil:motdepasse@db:5432/fournil"
PORT=3001
CORS_ORIGIN="http://localhost:5173"

# Optionnel : active POST /api/import/analyze (classification IA des feuilles
# d'un classeur non reconnu, déclenchée à la main depuis /import). Sans clé,
# l'import normal fonctionne sans changement ; le bouton "Essayer l'analyse IA"
# renverra juste une erreur claire si on clique dessus sans clé configurée.
ANTHROPIC_API_KEY=
